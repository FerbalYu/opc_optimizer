"""Backward-compatible package alias for historical imports.

Some tests and legacy tooling still import modules from ``local_optimizer``.
This package keeps those imports working while the codebase name is
``opc_optimizer``.
"""

